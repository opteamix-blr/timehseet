<g:select optionValue="chargeNumber"
          name="chargeCode.id"
          from="${task.chargeCodes}"
          optionKey="id"
          noSelection="${['null':'Select One...']}"
          >
</g:select>
